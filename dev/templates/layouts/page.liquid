<!DOCTYPE html>
<html lang="en">
  {% include 'base/head.liquid' %}
  <body class="body {{ class | default: 'page' }}" data-js="test">
    <main class="main {{ grid }}">
    {{ content }}
    </main>
  </body>
</html>
